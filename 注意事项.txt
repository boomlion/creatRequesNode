在sort事件中，不用接受，他是直接数组上操作的，如果接受，不很麻烦进行处理。
url.parse{进行get方式的文件数据接受 成数组}


在post的提交方式中，数据 存储在请求主题中 ，所以，在进行读取数据的时候要进行监听，
req.on('data',(chunk)=>{
外部存储的info
info+=chunk;
})
最后在进行读取;
数据的发送就是分段的，所以进行监听
queryString{就是post方式的进行数组重建，}